{
  "name": "erinnerungen-v1",
  "private": true,
  "version": "1.0.0",
  "description": "Offline-faehige Erinnerungen als Progressive Web App",
  "scripts": {
    "check:syntax": "node --check app.js && node --check service-worker.js",
    "check:smoke": "node scripts/smoke.mjs",
    "check": "npm run check:syntax && npm run check:smoke"
  }
}
