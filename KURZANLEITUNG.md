# Einkaufsliste App – Kurzanleitung

## 1) App starten
- App als PWA auf dem Handy installieren und als App starten (wichtig für die Mikrofonfunktion).
- Unten sollte eine Version stehen (z. B. `v1.0.38`).

## 2) Zwei Handys koppeln
- Auf beiden Handys denselben 4-stelligen `Code` setzen.
- Achtung: `0000` ist reserviert und nicht nutzbar.
- Nach dem Setzen sollten beide Geräte dieselbe Liste sehen.

## 3) Artikel hinzufügen
- In das Feld `Jede Zeile = ein Artikel` schreiben.
- Mit `Übernehmen` speichern.
- Mehrere Zeilen = mehrere Artikel.

## 4) Beim Einkaufen abhaken
- Unten auf `Einkaufen` wechseln.
- Artikel antippen = erledigt.
- Beim Wechsel zurück zu `Erfassen` werden erledigte Einträge entfernt.

## 5) Foto als Eintrag
- `Foto` tippen und ein Bild auswählen.
- Foto wird als Listeneintrag gespeichert.
- Auf `Foto öffnen` tippen, um das Bild groß zu sehen.

## 6) Weitere Buttons
- `🎤` Spracheingabe.
- `Löschen` leert das Eingabefeld.
- `Export` teilt/kopiert die Liste.
- `Update` lädt die neueste App-Version.

## 7) Wenn Sync nicht läuft
- Prüfen, ob auf beiden Geräten derselbe Code gesetzt ist.
- Internetverbindung prüfen.
- Einmal `Update` drücken und App neu laden.
